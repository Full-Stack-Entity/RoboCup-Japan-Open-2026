# 导航回归记录（2026-09-07）

本次提交包含阶段 3 导航实现和部分 Unity 动态回归结果，不代表全部比赛验收完成。

## 已验证路线

- LayoutA：客厅经厨房西侧入口往返成功；经厨房南侧第二入口进入成功；厨房到卧室成功。
- LayoutB：卧室到客厅由协调器自动执行，区域验证后发送 Room_reached；客厅到厨房及厨房到客厅垃圾桶候选位姿手动测试成功。
- LayoutC：客厅到 lobby 自动闭环成功；lobby 到卧室成功。卧室返回客厅曾在出口转向擦碰，修复回退路线丢失出门中心点的问题后复测成功，用户确认无碰撞。
- LayoutD / Session5：客厅到厨房自动闭环成功；厨房到卧室、卧室到 lobby 手动测试所有 Nav2 目标成功。后两条尚未获得用户明确的无擦碰确认。

手动测试通过 NavigateToPose 逐点执行 YAML 路线；它不等同于完整协调器目的地闭环或自动恢复验收。Nav2 SUCCEEDED 本身不证明全程无碰撞。

## 已知限制与后续工作

- 按用户指定，LayoutB lobby、LayoutC kitchen 暂不纳入可达性验收。
- LayoutC 出口修复已重新生成 layout_c.yaml。导入器现在在完整门洞路线不可用时保留可用的源房间门内/门外中心点，两点保持出门朝向，再连接普通路径。
- LayoutB living_room 的 trashbox_c03.001 可见正面经用户确认在原导出 forward 的左转 90 度方向。中心距离约 0.80 m 的目标位姿 (-3.794, -5.309, -1.571) 视觉上暂可接受；实际到达误差及机械臂可达性尚未验证。
- 按用户要求，垃圾桶方向和接近距离标定暂未写入导出器及 YAML，留待真实放置阶段验证后调整。
- 视觉、抓取、放置尚未完成；导航探测不会主动发送 Task_finished。
- 切换 Unity 布局前应完整停止旧导航、独立恢复节点和桥接节点，再先启动匹配布局的 ROS2，最后进入 Unity。当前协调器没有强制校验 Environment 与 Nav2 已加载地图是否一致；发现不一致时必须停止导航并重新初始化。
- 导航测试尚需补齐各方向、动态行人、故障恢复及超时回归。门洞候选和静态地图并不保证在动态环境下必然成功。

本仓库提交范围为 Ubuntu ROS2 工作树；Windows Assets/HandymanMapExporter 是外部目录，不包含在本次提交中。
