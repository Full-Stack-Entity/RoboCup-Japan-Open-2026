# 地图操作

> 不同电脑或目录发生变更时，视实际情况修改命令和路径。

## 1. 工具预热

打开 Ubuntu 终端：

```bash
cd ~/RoboCup-Japan-Open-2026
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 pkg prefix handyman_map_tools
ros2 pkg prefix handyman_rebuild_ros2
```

应出现：

```text
/home/crazylearner/RoboCup-Japan-Open-2026/install/handyman_map_tools
/home/crazylearner/RoboCup-Japan-Open-2026/install/handyman_rebuild_ros2
```

检查脚本：

```bash
ls -l import_handyman_maps.sh inspect_handyman_layout.sh
```

如果脚本没有执行权限：

```bash
chmod +x import_handyman_maps.sh inspect_handyman_layout.sh
```

运行：

```bash
./import_handyman_maps.sh /mnt/d/handyman-export
```

最终应出现（上方出现的 WARNING 不用管）：

```text
HANDYMAN MAP IMPORT PASSED
```

## 2. 获取 Unity 比赛工程

拿到 Unity 的比赛文件夹后，先用 Unity Hub 打开，使其编译成功。

## 3. 复制 Unity 工程

将 Unity 文件夹复制为两份：

- `A.handyman-unity2-work`
- `B.handyman-unity2-free`

## 4. 安装地图导出器

将 `HandymanMapExporter`（Unity 导出器）文件夹放置于：

```text
handyman-unity2-work\Assets\HandymanMapExporter
```

再打开：

```text
handyman-unity2-work\Build\Handyman.exe
```

## 5. 导出地图

打开：

```text
Tools → Handyman → Map Exporter
```

点击 `Scan Scene`，列表中应能看到比赛使用的所有布局。若扫描不到，说明接口出现变化，需要用 Codex 分析并修改导出器适配规则。

将导出器的输出目录设置为：

```text
某盘\handyman-export
```

推荐导出参数：

```text
Resolution                    0.05
Margin                        1.0
Room Boundary Padding         0.40
Ground Height                 0.0
Detect Ground Outside Rooms   开启
Ground Surface Tolerance      0.12
Minimum Obstacle Height       0.08
Maximum Obstacle Height       1.80
```

如果正式场景地面不在 Unity `Y=0`，需要将 `Ground Height` 改为实际地面高度。

点击：

```text
Export All Layouts
```

## 6. 制作自由视角版本

用 Codex 将 `handyman-unity2-free` 改为自由视角，要求如下：

1. 将机器人视角改为自由移动视角，通过 WASD、空格等按键在空间中自由移动，并用 F1、F2 等按键切换比赛场景。
2. 通过 `Build\Handyman.exe` 进入。
3. 强制自由相机占满整个屏幕，关闭机器人、电视等其他主屏幕相机，隐藏遮挡地图的比赛操作面板并停止计时。
4. 在屏幕中央增加醒目的绿色粗十字准星并使用黑色描边。准星指向带碰撞体的家具或物品时，在准星下方显示名称；名称优先使用物品根节点名称，最远识别距离为 100 米。
5. 根据房间区域实际的 X/Z 平面范围计算房间；准星指向不同区域时，在下方显示该区域所属的房间。
6. 按原始 Session 数据恢复物品位置和旋转，隐藏未使用物品和白色占位方块，并固定物品，避免观察时受物理影响再次掉落或漂移。
7. 若出现其他未知问题，根据现场具体情况分析。

## 7. 初步对照地图

示例：用资源管理器打开：

```text
D:\handyman-export\LayoutA\preview.png
```

将预览图与修改成功后的 `handyman-unity2-free` 自由视角进行对比。

## 8. 导入地图

在 Ubuntu 终端运行，例如：

```bash
cd ~/RoboCup-Japan-Open-2026
./import_handyman_maps.sh /mnt/d/handyman-export
```

## 9. 使用 RViz 逐套检查

另开一个 Ubuntu 终端，运行：

```bash
source /opt/ros/humble/setup.bash
source ~/RoboCup-Japan-Open-2026/install/setup.bash
rviz2 -f map
```

确保 RViz 打开后，再打开一个 Ubuntu 终端。运行以下命令切换地图，并逐一与自由视角进行对比：

```bash
cd ~/RoboCup-Japan-Open-2026
./inspect_handyman_layout.sh A
./inspect_handyman_layout.sh B
./inspect_handyman_layout.sh C
./inspect_handyman_layout.sh D
```
