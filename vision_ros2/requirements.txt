# requirements.txt — vision_ros2  (ROS 2 Humble / YOLOv12)
#
# YOLOv12 is distributed via the ultralytics package (>=8.3.0).
# Install with:  pip install -r requirements.txt

# Core inference — ultralytics>=8.3.0 ships YOLO12
ultralytics>=8.3.0

# Computer vision
opencv-python>=4.8.0
numpy>=1.24.0

# Image utilities
Pillow>=9.5.0

# TF transformations (replaces tf in ROS2)
tf-transformations>=0.0.1

# ROS2 Python client (installed via apt, listed for reference)
# rclpy          # apt: ros-humble-rclpy
# cv_bridge      # apt: ros-humble-cv-bridge
# sensor_msgs    # apt: ros-humble-sensor-msgs
# ament_index_python  # apt: ros-humble-ament-index-python
